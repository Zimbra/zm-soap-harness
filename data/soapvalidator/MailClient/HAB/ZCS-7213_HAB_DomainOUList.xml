<t:tests xmlns:t="urn:zimbraTestHarness">

	<t:property name="domain.name" value="test${TIME}${COUNTER}.com" />
	<t:property name="invalidDomain.name" value="test${TIME}${COUNTER}.com" />
	<t:property name="adminAcct1.name" value="test1${TIME}.${COUNTER}@${domain.name}" />
	<t:property name="ou1.name" value="ZimbraOU${COUNTER}1" />
	<t:property name="ou2.name" value="ZimbraOU${COUNTER}2" />
	<t:property name="ou3.name" value="ZimbraOU${COUNTER}3" />
	<t:property name="hab.notes" value="Group created notes" />

	<t:test_case testcaseid="Ping" type="always">
		<t:objective>basic system check</t:objective>

		<t:test required="true">
			<t:request>
				<PingRequest xmlns="urn:zimbraAdmin" />
			</t:request>
			<t:response>
				<t:select path="//admin:PingResponse" />
			</t:response>
		</t:test>
	</t:test_case>

	<t:test_case testcaseid="CreateAccount/Domain" type="always">
		<t:objective>Account Setup</t:objective>
		<t:steps>
			1. Login into admin.
			2. Create test accounts.
		</t:steps>

		<t:test id="admin_login" required="true">
			<t:request>
				<AuthRequest xmlns="urn:zimbraAdmin">
					<name>${admin.user}</name>
					<password>${admin.password}</password>
				</AuthRequest>
			</t:request>
			<t:response>
				<t:select path="//admin:AuthResponse/admin:authToken" set="authToken" />
			</t:response>
		</t:test>

		<t:test required="true">
			<t:request>
				<CreateDomainRequest xmlns="urn:zimbraAdmin">
					<name>${domain.name}</name>
					<a n="zimbraNotes">test of adding an OU</a>
				</CreateDomainRequest>
			</t:request>
			<t:response>
				<t:select path="//admin:CreateDomainResponse/admin:domain"
					attr="id" set="domain.id" />
			</t:response>
		</t:test>

		<!-- Create an admin account under this domain -->
		<t:test required="true">
			<t:request>
				<CreateAccountRequest xmlns="urn:zimbraAdmin">
					<name>${adminAcct1.name}</name>
					<password>${defaultpassword.value}</password>
				</CreateAccountRequest>
			</t:request>
			<t:response>
				<t:select path="//admin:CreateAccountResponse/admin:account"
					attr="id" set="account1.id" />
			</t:response>
		</t:test>
	</t:test_case>

	<t:test_case testcaseid="CreateDomainOUs" type="sanity"
		bugids="ZCS-7213">
		<t:objective>Create a multiple OUs in a domain</t:objective>
		<t:steps>
			1. Create a 3 HAB OUs under domain ${domain.name}.
			2. Fire
			HABOrgUnitRequest to create 3 OUs in domains
			3. Verify request should succeed.
		</t:steps>

		<t:test>
			<t:request>
				<HABOrgUnitRequest op="create" name="${ou1.name}"
					xmlns="urn:zimbraAdmin">
					<domain by="name">${domain.name}</domain>
				</HABOrgUnitRequest>
			</t:request>
			<t:response>
				<t:select path="//admin:HABOrgUnitResponse//admin:habOrgUnitName"
					match="${ou1.name}" />
			</t:response>
		</t:test>

		<t:test>
			<t:request>
				<HABOrgUnitRequest op="create" name="${ou2.name}"
					xmlns="urn:zimbraAdmin">
					<domain by="name">${domain.name}</domain>
				</HABOrgUnitRequest>
			</t:request>
			<t:response>
				<t:select path="//admin:HABOrgUnitResponse//admin:habOrgUnitName"
					match="${ou2.name}" />
			</t:response>
		</t:test>

		<t:test>
			<t:request>
				<HABOrgUnitRequest op="create" name="${ou3.name}"
					xmlns="urn:zimbraAdmin">
					<domain by="name">${domain.name}</domain>
				</HABOrgUnitRequest>
			</t:request>
			<t:response>
				<t:select path="//admin:HABOrgUnitResponse//admin:habOrgUnitName"
					match="${ou3.name}" />
			</t:response>
		</t:test>
	</t:test_case>

	<t:test_case testcaseid="ListDomainOUs_1" type="sanity"
		bugids="ZCS-7213">
		<t:objective>Fire HABOrgUnitRequest to list OUs for above domain</t:objective>
		<t:steps>
			1. Fire HABOrgUnitRequest - list op, for the domain.name 
			2. Verify response should contain the 3 OUs created above
		</t:steps>
		<t:test>
			<t:request>
				<HABOrgUnitRequest op="list" 
					xmlns="urn:zimbraAdmin">
					<domain by="name">${domain.name}</domain>
				</HABOrgUnitRequest>
			</t:request>
			<t:response>
				<t:select path="//admin:HABOrgUnitResponse//admin:habOrgUnitName"
					match="${ou1.name}" />
				<t:select path="//admin:HABOrgUnitResponse//admin:habOrgUnitName"
					match="${ou2.name}" />
				<t:select path="//admin:HABOrgUnitResponse//admin:habOrgUnitName"
					match="${ou3.name}" />
			</t:response>
		</t:test>
	</t:test_case>

	<t:test_case testcaseid="ListDomainOUs_2" type="sanity"
		bugids="ZCS-7213">
		<t:objective>Fire HABOrgUnitRequest to list OUs for a non-existent domain</t:objective>
		<t:steps>
			1. Fire HABOrgUnitRequest - list op, for the invalidDomain.name which does not exists 
			2. Verify response should give an error as domain not exists.
		</t:steps>
		<t:test>
			<t:request>
				<HABOrgUnitRequest op="list" 
					xmlns="urn:zimbraAdmin">
					<domain by="name">${invalidDomain.name}</domain>
				</HABOrgUnitRequest>
			</t:request>
			<t:response>
				<t:select path="//zimbra:Code" match="^account.NO_SUCH_DOMAIN" />
			</t:response>
		</t:test>
	</t:test_case>
</t:tests>
