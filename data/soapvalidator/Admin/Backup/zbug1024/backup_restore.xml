<t:tests xmlns:t="urn:zimbraTestHarness">
<t:property name="folder.custom" value="${ZimbraQARoot}/${testMailRaw.root}/zbug1024/backup"/>
<t:property name="domain1.name" value="zcs804.zimbra.com"/>
<t:property name="test_account1.name" value="account1@zcs804.zimbra.com"/>
<t:property name="test_account1.password" value="z1mbr4@123"/>
<t:property name="server.zimbraAdmin" value="${zimbraServer.name}"/>

<t:test_case testcaseid="Ping" type="always" >
    <t:objective>Basic system check</t:objective>

    <t:test id="ping" required="true">
        <t:request>
            <PingRequest xmlns="urn:zimbraAdmin"/>
        </t:request>
        <t:response>
            <t:select path="//admin:PingResponse"/>
        </t:response>
    </t:test>
</t:test_case>

<t:test_case testcaseid="acct1_setup" type="always" >
    <t:objective>Account Setup</t:objective>
    <t:steps> 1. Login to the admin account
              2. Create Domain.
    </t:steps>


    <!-- Create folder if not exists -->
    <t:staftask>
        <t:request>
            <server>${zimbraServer.name}</server>
            <service>PROCESS</service>
            <params>START SHELL COMMAND "mkdir -p ${folder.custom}" RETURNSTDOUT RETURNSTDERR WAIT ${staf.process.timeout.default}</params>
        </t:request>
    </t:staftask>

    <!-- Small delay after folder creation -->
    <t:delay msec="1500"/>

    <!-- Fix permissions -->
    <t:staftask>
        <t:request>
            <server>${zimbraServer.name}</server>
            <service>PROCESS</service>
            <params>START SHELL COMMAND "chmod 777 ${folder.custom}" RETURNSTDOUT RETURNSTDERR WAIT ${staf.process.timeout.default}</params>
        </t:request>
    </t:staftask>

    <t:delay msec="1000"/>


    <t:property name="server.zimbraAdmin" value="${zimbraServer.name}"/>
    <t:test id="admin_login" required="true">
        <t:request>
            <AuthRequest xmlns="urn:zimbraAdmin">
                <name>${admin.user}</name>
                <password>${admin.password}</password>
            </AuthRequest>
        </t:request>
        <t:response>
            <t:select path="//admin:AuthResponse/admin:authToken" set="authToken"/>
        </t:response>
    </t:test>
    <t:test>
        <t:request>
            <CreateDomainRequest xmlns="urn:zimbraAdmin">
                <name>${domain1.name}</name>
            </CreateDomainRequest>
        </t:request>
        <t:response>
            <t:select path="//admin:CreateDomainResponse/admin:domain" attr="id" set="domain1.id" />
        </t:response>
    </t:test>
</t:test_case>

<t:test_case testcaseid="RestoreRequest" type="bhr"  areas="backup">
    <t:objective>Restore the full backup from zcs804 version backup folder </t:objective>

    <t:test>
        <t:request>
            <RestoreRequest xmlns="urn:zimbraAdmin">
                <restore target="${folder.custom}" includeIncrementals="0" method="mb"> 
                    <account name="${test_account1.name}"/>
                </restore>
            </RestoreRequest>
        </t:request>
        <t:response>
            <t:select path="//admin:RestoreResponse"/>
        </t:response>
    </t:test>
    <t:delay msec="3000" />
    <t:test id="getAccountInfoRequest1a">
        <t:request>
            <GetAccountInfoRequest xmlns="urn:zimbraAccount">
                <account by="name">${test_account1.name}</account>
            </GetAccountInfoRequest>
        </t:request>
        <t:response>
            <t:select path="//acct:GetAccountInfoResponse/acct:name" match="${test_account1.name}"/>
            <t:select path="//acct:GetAccountInfoResponse/acct:attr[@name='zimbraId']" set="test_account1.id"/>
        </t:response>
    </t:test>
    <t:delay msec="2000" />
    <t:test>
        <t:request>
            <DeleteAccountRequest xmlns="urn:zimbraAdmin" id="${test_account1.id}">
            </DeleteAccountRequest>
        </t:request>
        <t:response>
            <t:select path="//admin:DeleteAccountResponse" />
        </t:response>
    </t:test>
    <t:test>
        <t:request>
            <DeleteDomainRequest xmlns="urn:zimbraAdmin" id="${domain1.id}">
            </DeleteDomainRequest>
        </t:request>
        <t:response>
            <t:select path="//admin:DeleteDomainResponse" />
        </t:response>
    </t:test>
</t:test_case>
</t:tests>