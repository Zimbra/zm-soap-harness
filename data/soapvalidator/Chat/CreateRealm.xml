<t:tests xmlns:t="urn:zimbraTestHarness">
    <namespace prefix="admin" uri="urn:zimbraAdmin"/>
    <t:property name="domain0.name" value="${TIME}${COUNTER}"/>
    <t:test_case testcaseid="Ping" type="chat" >
        <t:objective>basic system check</t:objective>
        <t:test id="ping">
            <t:request>
                <PingRequest xmlns="urn:zimbraAdmin"/>
            </t:request>
            <t:response>
                <t:select path="//admin:PingResponse"/>
            </t:response>
        </t:test>

    </t:test_case>
    <t:test_case testcaseid="admin_login" type="chat" >
        <t:objective>Login to Admin Account</t:objective>

        <t:test id="admin_login" required="true" >
            <t:request>
                <AuthRequest xmlns="urn:zimbraAdmin">
                    <name>${admin.user}</name>
                    <password>${admin.password}</password>
                </AuthRequest>
            </t:request>
            <t:response>
                <t:select path="//admin:AuthResponse/admin:authToken" set="authToken"/>
            </t:response>
        </t:test>
    </t:test_case>

    <t:test_case testcaseid="CreateDomainRequest" type="chat">
        <t:objective>create a Domain</t:objective>
        <t:steps>
            1. Create a new Domain with ChatEnabled
        </t:steps>

        <t:test id="CreateDomainRequest0a">
            <t:request>
                <CreateDomainRequest xmlns="urn:zimbraAdmin">
                    <name>domain${domain0.name}.com</name>
                    <a n="zimbraFeatureZulipChatEnabled">TRUE</a>
                    <a n="zimbraChatJwtSecret">zimbra123</a>
                </CreateDomainRequest>
            </t:request>
            <t:response>
                <t:select path="//admin:CreateDomainResponse/admin:domain" attr="id" set="domain0.id"/>
            </t:response>
        </t:test>
    </t:test_case>

    <t:test_case testcaseid="VerifyAccountOnChatServer" type="chat" depends="CreateDomainRequest">
        <t:objective>Verify Account on chat server</t:objective>
        <t:test id="VerifyAccountOnChatServer">
            <t:request chat="true" action="listRealms" jwt="" domainId="" >

            </t:request>
            <t:response>
                <t:select path="//admin:ChatResponse/admin:members/admin:member/admin:email" match=".*domain${domain0.name}.*"/>
            </t:response>
        </t:test>
    </t:test_case>
</t:tests>